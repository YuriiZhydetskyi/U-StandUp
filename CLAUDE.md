# U-StandUp — Сайт українського стендап-клубу в Німеччині

Сайт для клубу "У Стендап" (ustandup.club) — статичний сайт з подіями (стендап-концерти, відкриті мікрофони, читки). Мова контенту — українська.

## Tech Stack

- Vanilla JS (ES modules у браузері, CommonJS у Node-скриптах)
- Tailwind CSS v4 (CLI-based, не PostCSS)
- Node.js build system (custom `scripts/build-dist.js`)
- YAML для даних про події → компілюється в JS/JSON при білді
- Sharp для оптимізації зображень (responsive WebP)
- GitHub Pages для деплою

## Commands

- `npm run build` — повний білд (favicons → build-dist → tailwind CSS)
- `npm run start` — локальний сервер із dist/
- `npm run build:css` — тільки Tailwind CSS
- `npm run watch:css` — Tailwind watch mode
- `npm run optimize-images` — оптимізація зображень окремо
- `npm run convert-events` — конвертація подій в YAML (міграційний скрипт)

## Структура проєкту

- `src/events/*.yaml` — події (YAML, одна подія = один файл)
- `src/img/` — афіші подій та логотипи (WebP)
- `src/js/` — фронтенд-код (app.js, components/, utils/)
- `src/data/constants.json` — категорії, місяці, дні тижня (укр)
- `src/data/venues.yaml` — довідник локацій (адреси, maps-лінки)
- `scripts/build-dist.js` — головний білд-скрипт
- `dist/` — зібраний сайт (не редагувати вручну, генерується білдом)

## Додавання нової події

1. Створи файл `src/events/YYYY-MM-DD-назва-місто.yaml`
2. Використовуй формат із існуючих подій (id, name, category, date, time, location, geo, seo, description)
3. Категорії: `concert`, `open-mic`, `workshop`
4. Для локації — бери дані з `src/data/venues.yaml` (адреса, geo, linkToMaps)
5. Афішу додай у `src/img/` у форматі WebP, назва: `YYYY-MM-DD-назва.webp`
6. `isFavorite: true` — подія показується в hero-секції на головній
7. Після створення — запусти `npm run build` для генерації сторінок

## Формат YAML події (обов'язкові поля)

```yaml
id: YYYY-MM-DD-назва-місто
name: "Назва події"
category: concert|open-mic|workshop
date: "YYYY-MM-DD"
time: "HH:MM"
location: Назва закладу, Місто
image: img/назва-файлу.webp
geo:
  region: DE-NW
  placename: Місто
  name: Назва закладу
  streetAddress: Вулиця та номер
  addressLocality: Місто
  postalCode: "XXXXX"
  addressCountry: DE
seo:
  description: "Короткий опис для SEO"
description: |-
  Повний опис події (підтримує HTML-теги для посилань)
```

## Важливі правила

- НІКОЛИ не редагуй файли у `dist/` — вони перегенеровуються при білді
- Контент тільки українською мовою
- Зображення тільки у форматі WebP
- Ціни завжди в EUR
- `locationForCalendar` — якщо відрізняється від `location` (для Google/Apple Calendar)
- `linkToMaps` — завжди Google Maps short link
- Описи подій підтримують HTML (`<a>` для Instagram-посилань тощо)
- Emoji використовуються в описах подій (📅🕖📍🔥 тощо)

## Локації

Дані про всі локації зберігаються у `src/data/venues.yaml`. Коли додаєш нову подію — бери geo-дані звідти. Основні локації:
- **Filmdose, Köln** — Theatercafé Filmdose, Zülpicher Str. 39
- **Nowa Mowa, Bonn** — Paulstraße 5, ресторан української кухні
- **Wunderhaus, Düsseldorf** — Kindercafé, Kölner Landstraße 148
- **Shadow-Arkaden, Düsseldorf** — Schadowstraße 11
